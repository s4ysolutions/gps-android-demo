package s4y.demo.mapsdksdemo.gps.tests.filters.kalman

import org.junit.Test
import kotlin.math.cos
import kotlin.math.sin
import kotlin.test.assertEquals

class ProjectionsTest {
    /*
    @Test
    fun toXtoY_shouldBeInvertible() {
        // Arrange
        // v is a vector with module 5m/s and bearing 15 degrees
        val v = 5.0f // 5m/s
        val bearing = 15.0f // degrees
        val cosBearing = cos(Math.toRadians(bearing.toDouble()))
        val sinBearing = sin(Math.toRadians(bearing.toDouble()))
        // Act
        // project v to x and y
        val vx = v.toX(cosBearing)
        val vy = v.toY(sinBearing)
        val vHat = moduleDegreesToMeters(vx, vy)
        // Assert
        assertEquals(v, vHat)
    }
     */
}